<%= form_for @landlord do |f| %>

	<% if @landlord.errors.any? %>
		<div class="error_explanation">
			<h2>
					<%= pluralize(@landlord.errors.count, 'error') %> prohibited 
					this landlord from beign saved:
			</h2>
			<ul>
				<% @landlord.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>
	<p>
		<%= f.label :firstname %>
		<%= f.text_field :firstname %>
	</p>

	<p>
		<%= f.label :lastname %>
		<%= f.text_field :lastname %>
	</p>

	<p>
		<%= f.label :phone %>
		<%= f.text_field :phone %>
	</p>

	<p>
		<%= f.label :email %>
		<%= f.text_field :email %>
	</p>

	<p>
		<%= f.submit %>
	</p>
<% end %>